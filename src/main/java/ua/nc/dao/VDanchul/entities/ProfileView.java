package ua.nc.dao.VDanchul.entities;

/**
 * Created by Rangar on 26.04.2016.
 */
/*public class ProfileView {
    private Profile
    public ProfileView(Profile profile) {}

    public JSON getProfileData() {
        JSON result = new JSON();

        for (Field field in this.profile.getFields()) {
            FieldValue fieldValue = this.profile.getFieldValue(field);

            JSON fieldResult = new JSON();
            fieldResult.set("id", field.id);
            fieldResult.set("type", );
            // for other fields too

        }
    }
    public void setProfileData(JSON input) {
        for (JSON fieldJson in input.getArray()){
            this.profile.setField(fieldJson.get("id"), fieldJson.get("value"));
        }
        this.profile.save();
    }
}*/
